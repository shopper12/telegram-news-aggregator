name: Telegram and Kakao news briefing

on:
  workflow_dispatch:
    inputs:
      hours:
        description: "Hours to scan"
        required: false
        default: "1"
      limit:
        description: "Max items to classify; 999 means effectively uncapped"
        required: false
        default: "999"
      briefing_kind:
        description: "Briefing kind"
        required: false
        default: "regular"
  schedule:
    # GitHub Actions cron uses UTC. KST is UTC+9.
    # KST 07:00, 07:30, 08:00, 08:30.
    - cron: "0,30 22-23 * * *"
    # KST 09:00 through 22:30, every 30 minutes.
    - cron: "0,30 0-13 * * *"
    # KST 23:00.
    - cron: "0 14 * * *"

# Scheduled runs execute every day, including weekends and holidays.
# GitHub-hosted scheduled jobs may start several minutes later than the nominal cron time.
concurrency:
  group: telegram-news-briefing
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  send-briefing:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      PYTHONPATH: src
      TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
      TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
      TELEGRAM_STRING_SESSION: ${{ secrets.TELEGRAM_STRING_SESSION }}
      TELEGRAM_SESSION_NAME: telegram_news_session
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_TARGET_CHAT_ID: ${{ secrets.TELEGRAM_TARGET_CHAT_ID }}
      TELEGRAM_TARGET_CHAT_IDS: ${{ secrets.TELEGRAM_TARGET_CHAT_IDS }}
      TELEGRAM_SEND_ENABLED: "1"
      DATABASE_URL: sqlite:///data/news.db
      CHANNEL_CONFIG: config/channels.yaml
      TIMEZONE: Asia/Seoul
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      GEMINI_MODEL: gemini-2.5-flash
      # Telegram delivery is always performed by telegram_dispatch.
      # Setting kakao here additionally sends the same report to KakaoTalk memo.
      NOTIFIER: kakao
      KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
      KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
      KAKAO_CLIENT_SECRET: ${{ secrets.KAKAO_CLIENT_SECRET }}
      KAKAO_WEB_URL: ${{ secrets.KAKAO_WEB_URL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Resolve briefing parameters
        id: cadence
        run: |
          python - <<'PY'
          import json
          import os
          from datetime import datetime, timezone
          from pathlib import Path
          from zoneinfo import ZoneInfo

          event_name = os.getenv("GITHUB_EVENT_NAME", "")
          event_path = os.getenv("GITHUB_EVENT_PATH", "")
          schedule = ""
          inputs = {}
          if event_path and Path(event_path).exists():
              with open(event_path, "r", encoding="utf-8") as f:
                  payload = json.load(f)
              schedule = str(payload.get("schedule") or "").strip()
              inputs = payload.get("inputs", {}) or {}

          now_utc = datetime.now(timezone.utc)
          now_kst = now_utc.astimezone(ZoneInfo("Asia/Seoul"))

          hours = inputs.get("hours") or "1"
          limit = inputs.get("limit") or "999"
          briefing_kind = inputs.get("briefing_kind") or "regular"
          reason = "manual"

          if event_name == "schedule":
              hours = "1"
              limit = "999"
              briefing_kind = "regular"
              reason = "kst_0700_to_2300_every_30_minutes"

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as out:
              out.write(f"hours={hours}\n")
              out.write(f"limit={limit}\n")
              out.write(f"briefing_kind={briefing_kind}\n")
              out.write(f"kst_now={now_kst:%Y-%m-%d %H:%M:%S KST}\n")
              out.write(f"schedule={schedule}\n")

          print(f"KST now: {now_kst:%Y-%m-%d %H:%M:%S KST}")
          print(f"event={event_name} schedule={schedule}")
          print(f"reason={reason} hours={hours} limit={limit} briefing_kind={briefing_kind}")
          PY

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Validate Telegram and Kakao secrets
        run: |
          python - <<'PY'
          import os
          import sys
          import requests
          from telethon.sessions import StringSession

          def clean_secret(value):
              if not value:
                  return ""
              return "".join(str(value).strip().strip('"\'').split())

          required = [
              "TELEGRAM_API_ID",
              "TELEGRAM_API_HASH",
              "TELEGRAM_STRING_SESSION",
              "TELEGRAM_BOT_TOKEN",
              "KAKAO_REST_API_KEY",
              "KAKAO_REFRESH_TOKEN",
          ]
          if not clean_secret(os.getenv("TELEGRAM_TARGET_CHAT_ID")) and not clean_secret(os.getenv("TELEGRAM_TARGET_CHAT_IDS")):
              print("Missing Telegram target: set TELEGRAM_TARGET_CHAT_ID or TELEGRAM_TARGET_CHAT_IDS.")
              sys.exit(1)

          missing = [name for name in required if not clean_secret(os.getenv(name))]
          if missing:
              print("Missing GitHub Actions secrets: " + ", ".join(missing))
              sys.exit(1)

          try:
              StringSession(clean_secret(os.getenv("TELEGRAM_STRING_SESSION")))
          except Exception as exc:
              print("Invalid TELEGRAM_STRING_SESSION secret.")
              print("Reason: " + type(exc).__name__ + ": " + str(exc))
              sys.exit(1)

          bot_token = clean_secret(os.getenv("TELEGRAM_BOT_TOKEN"))
          try:
              response = requests.get(
                  f"https://api.telegram.org/bot{bot_token}/getMe",
                  timeout=20,
              )
              payload = response.json() if response.content else {}
          except Exception as exc:
              print("Telegram Bot API authorization check failed before the briefing.")
              print("Reason: " + type(exc).__name__ + ": " + str(exc))
              sys.exit(1)

          if response.status_code != 200 or not payload.get("ok"):
              description = str(payload.get("description") or response.text or "unknown Telegram API error")
              print("Invalid or revoked GitHub Actions secret: TELEGRAM_BOT_TOKEN.")
              print(f"Telegram getMe returned HTTP {response.status_code}: {description}")
              print("Replace TELEGRAM_BOT_TOKEN in both GitHub Actions and Render with the same fresh BotFather token.")
              sys.exit(1)

          bot = payload.get("result") or {}
          print("Telegram bot token verified for @" + str(bot.get("username") or "unknown"))
          print("KakaoTalk delivery secrets are present.")
          PY

      - name: Show delivery mode
        run: |
          python - <<'PY'
          import os
          target_present = bool(os.getenv("TELEGRAM_TARGET_CHAT_ID") or os.getenv("TELEGRAM_TARGET_CHAT_IDS"))
          print("Telegram delivery: enabled")
          print("Telegram target chat id(s): " + ("present" if target_present else "missing"))
          print("KakaoTalk memo delivery: enabled")
          print("Kakao REST API key: " + ("present" if os.getenv("KAKAO_REST_API_KEY") else "missing"))
          print("Kakao refresh token: " + ("present" if os.getenv("KAKAO_REFRESH_TOKEN") else "missing"))
          PY

      - name: Run news briefing and send notifications
        env:
          BRIEFING_KIND: ${{ steps.cadence.outputs.briefing_kind }}
        run: |
          mkdir -p data reports
          HOURS="${{ steps.cadence.outputs.hours || '1' }}"
          LIMIT="${{ steps.cadence.outputs.limit || '999' }}"
          python scripts/run_once.py run --hours "$HOURS" --limit "$LIMIT" --send

      - name: Commit latest report cache
        run: |
          if [ ! -f reports/latest_report.json ]; then
            echo "latest_report.json not found; skip commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f reports/latest_report.json reports/latest_report.md
          if git diff --cached --quiet; then
            echo "No latest report changes to commit"
            exit 0
          fi
          git commit -m "Update latest news report cache [skip ci]"
          git pull --rebase
          git push

      - name: Upload report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: telegram-news-reports
          path: reports/*.md
          if-no-files-found: ignore
